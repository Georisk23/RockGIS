# This makefile is for compiling RockGIS from linux
# to be used on a linux system

CC=g++
CFLAGS=-std=c++11 -lm -O3 -w
SOURCE:= $(wildcard src/*.cpp)
EXE=RockGIS_Linux

all:
	$(CC) $(CFLAGS) $(SOURCE) -o $(EXE)
